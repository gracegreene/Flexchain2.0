#https://machinelearningmastery.com/arima-for-time-series-forecasting-with-python/
from pandas import read_csv
from pandas import datetime
from matplotlib import pyplot
from statsmodels.tsa.arima_model import ARIMA
from sklearn.metrics import mean_squared_error


def forecast_next1():

    return [sku,forecast]

def forecast_next2():
    return [sku, forecast]

def forecast_next3():
    return [sku, forecast]

def calc_ROP(sku):
    return ROP

def get_curr_inventory(sku):
    #sum all inventory from all stores and warehouse
    return quantity

def getstock_status(sku):
    #if calc_ROP(sku) < available inventory
        #if available inventory = 0
            #then return 0
        #else
            #return 1
    #else
        #return 2


def get_order(sku):

    return amount, location id
